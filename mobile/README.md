# Radio Station – Mobile App (Flutter)

Flutter app that consumes the radio station backend API and plays HLS streams.

**Status:** Placeholder. Create Flutter project here when ready.

- API: same as web (backend `GET /streams`, HLS playlist URLs).
- Use an HLS-capable player package for Flutter.
