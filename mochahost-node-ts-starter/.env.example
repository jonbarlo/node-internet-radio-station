# App (use for both .env locally and .env.prod for production)
NODE_ENV=development
PORT=3000

# FTP – only needed for deploy-mochahost (put real values in .env.prod)
FTP_HOST=ftp.yourhost.com
FTP_USER=your_ftp_user
FTP_PASSWORD=your_ftp_password
FTP_PORT=21
FTP_SECURE=false
FTP_REMOTE_PATH=/
